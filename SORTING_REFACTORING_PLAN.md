# План рефакторинга сортировки и фильтрации

## Цель
Заменить текущую реализацию через `ui.collapsing` на кнопку с всплывающим окном для более чистого UI.

## Текущее состояние
В `articles_list.rs` используется `ui.collapsing("Сортировка и фильтрация", ...)` который занимает много места и выглядит неэстетично.

## План работ

### 1. Добавить состояние для модального окна
- [ ] Добавить поле `show_filter_popup: bool` в структуру `ArticlesList`
- [ ] Инициализировать в `new()` со значением `false`

### 2. Создать UI для кнопки фильтра
- [ ] Добавить кнопку с иконкой (🔧 или ⚙) справа от поисковой строки
- [ ] При клике устанавливать `show_filter_popup = true`
- [ ] Отображать индикатор активных фильтров (например, изменение цвета кнопки)

### 3. Реализовать модальное окно фильтрации
- [ ] Создать метод `filter_popup_ui(&mut self, ui: &mut Ui, ctx: &Context)`
- [ ] Использовать `egui::Window` или `egui::Area` для модального окна
- [ ] Перенести в окно:
  - Выбор сортировки (Новые/Лучшие)
  - Выбор периода (при сортировке "Лучшие")
  - Выбор сложности (Все/Простой/Средний/Сложный)
  - Кнопку "Применить" (закрывает окно и применяет фильтры)
  - Кнопку "Отмена" (закрывает окно без применения)
  - Закрытие по клику вне области окна

### 4. Реализовать отдельное окно для поиска
- [ ] При активном поиске показывать другую кнопку/тот же попап
- [ ] В окне поиска: сортировка (релевантность/дата/рейтинг)

### 5. Удалить старую реализацию
- [ ] Удалить `ui.collapsing` блок
- [ ] Удалить закомментированный код
- [ ] Очистить неиспользуемые импорты если появятся

### 6. Дополнительные улучшения (опционально)
- [ ] Добавить анимацию появления/исчезновения окна
- [ ] Сохранять позицию скролла при открытии окна
- [ ] Добавить превью выбранных фильтров на кнопке

## Файлы для изменения
- `src/views/articles_list.rs` - основные изменения

## Примечания
- Окно закрывается по клику вне области, по кнопке "Применить" или "Отмена"
- Применение фильтров должно перезагружать список статей
- UI должен быть адаптивным для мобильных устройств (ОС Аврора)
